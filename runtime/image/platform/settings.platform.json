{
  "_comment": "Locked platform settings layer. Baked read-only into the image and merged ahead of the tenant's own ~/.claude/settings.json. Full security hooks (nested-claude guard, shellfirm, deny rules) are populated at M4. The Stop hook here is M2.5 metering (emits per-turn token usage to the audit-collector; not an LLM call).",
  "permissions": {
    "deny": []
  },
  "hooks": {
    "Stop": [
      {
        "hooks": [
          { "type": "command", "command": "node /opt/platform/hooks/metering-stop-hook.mjs" }
        ]
      }
    ]
  }
}

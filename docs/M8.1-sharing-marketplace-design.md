# M8.1 — Обмен / маркетплейс артефактов (детальный дизайн, design-first)

Статус: **ON REVIEW** (код не пишу до твоего OK). Конкретизирует [12-artifacts-sharing.md](12-artifacts-sharing.md)
в buildable-спеку под наш текущий стек. Пилот — **vitaliy + dmrudenko** (оба уже на
платформе); раскатка на остальные 6 ботов = добивка M7, ПОСЛЕ M8 (порядок исполнения, см.
[06-build-overview.md](06-build-overview.md) «Уточнение порядка исполнения 2026-06-16»).

---

## 1. Цель и критерий «готово»
Тенант может **опубликовать** свой артефакт (`.claude/`-файлы) в общий реестр и **импортировать**
чужой — детерминированно, с обязательными сканами и неизменяемым аудитом, без того чтобы
пользователь сам пушил в Git. Гейт M8 (из doc 06/12):
- workflow из билдера компилируется в валидные `commands/`+`agents/` (✅ уже умеем — M5.9);
- публикация без pass сканеров невозможна; PR создаёт агент; провенанс в реестре;
- импорт делает повторный скан и ставит **зафиксированную версию**; fail → отказ с причиной;
- все вердикты — в append-only аудите.

## 2. Что УЖЕ есть (переиспользуем, не строим заново)
| Готовый компонент | Где | Роль в M8 |
|---|---|---|
| Таблицы `artifacts / artifact_versions / scan_results / installs` | `packages/db/src/schema.ts:99-140` | модель данных реестра (уже в схеме с M1) |
| enum `artifact_type` (skill/subagent/command/mcp/workflow/plugin) + `scanner_kind`, `verdict_kind` | schema.ts:32-46 | типы и вердикты |
| `@fleet/scanners` — `scanArtifact(deps,input)` + `httpJudgeClient`, fan-out по типу, worst-verdict-wins, **fail-closed** | `packages/scanners/` | единый скан-движок публикации/импорта |
| Judge Orchestrator (кэш `judge_verdicts` по hash+ruleset+model, single-pass, $-cap) | `apps/judge-orchestrator/` | LLM-судья — **только событийно** (publish/import), не по расписанию |
| Scan→judge→approval→apply паттерн | `apps/api/src/mcp-gate.ts`, `approval-routes.ts` | калька для publish/import |
| Approvals (M5.4b): Telegram notify по токену тенанта + ответ в Mini App | `apps/api/src/approvals.ts` | подтверждение публикации/импорта |
| Mini App: screen-router, scoped FileTree (M5.10), WorkflowBuilder (M5.9), i18n `t()` | `apps/miniapp/` | UI «опубликовать»/«маркетплейс»/«импорт» |
| git-PAT через OneCLI-прокси (под пушит по https, PAT инжектится прокси) | [[feedback_pod_has_no_git_push]] | основа для агентского PR в org |
| WorkflowBuilder + компилятор | M5.9 | тип `workflow` публикуется как `commands/`+`agents/` |

**Вывод:** ~60% M8 уже лежит. Net-new — реестр-роуты, GitHub-org PR-пайплайн, маркетплейс-UI.

## 3. Scope MVP (намеренно срезаю риск)
- **Типы артефактов MVP:** `skill`, `subagent`, `command`, `workflow` — чистые файлы, низший риск,
  покрыты Skill Scanner + deterministic. `mcp` — импорт MCP УЖЕ закрыт существующим mcp-gate
  (M5.5), в маркетплейс заводим его шарингом записи `.mcp.json` ПОЗЖЕ. `plugin` — **DEFER**
  (highest-risk: все три сканера + AgentShield; отдельный под-этап M8.3).
- **Видимость MVP:** `public` + `private` (selective/«поделиться выборочно» — позже; колонка уже есть).
- **Хранилище MVP:** репо `joejoker77/claude-bot-skills` = source of truth артефактов (`git_ref` в
  `artifact_versions`), см. §4. **MinIO/объектное зеркало — DEFER** (РЕШЕНО: да, Vitaliy 2026-06-16).
- **Аппрув на ПУБЛИКАЦИЮ — только для НЕ-админов** (РЕШЕНО 2026-06-16): админ-боты (vitaliy,
  dmrudenko) публикуют свой артефакт без approval-карточки; обычные тенанты — обязательный approval.
  Гейтится по `users.is_admin`. **Импорт — approval ВСЕГДА** (пересечение границы доверия), для всех.
- **Пилот:** только vitaliy + dmrudenko. Флаг тенанта гейтит фичу (как все M5/M6).

## 4. Стор = существующий репо `joejoker77/claude-bot-skills` + REST-API PR (РЕШЕНО 2026-06-16)
Vitaliy: стор — отдельный маркетплейс на **уже используемом** репо `joejoker77/claude-bot-skills`
(не новая org, не fleet-platform). Преимущество: у репо **уже есть CI-сканер-гейты на каждый PR**
(signature-scanner, sensitive-paths-gate, rate-limit-gate) + auto-merge, и паттерн `skill-author`
(branch→commit→PR). M8 переиспользует это, а не строит параллельный пайплайн.

- **PAT уже в OneCLI и проверен (2026-06-16):** секрет `vitaliy-github-github_pat`, host-pattern
  **`api.github.com`**, bound к `vitaliy-bot`. Проба через прокси: `GET /user`→`joejoker77`,
  `GET /repos/joejoker77/claude-bot-skills`→`permissions {admin, push, pull, maintain, triage}=true`.
  Значение токена в поде не видно (инжектится egress-прокси). НЕ путать с `vitaliy-git-fleet-platform`
  (host `github.com`, fine-grained, только fleet-platform Contents — для self-push, НЕ для M8) —
  разные host-pattern → НЕ шадовят друг друга.
- **Весь публикационный пайплайн — через GitHub REST API на `api.github.com`** (без git-push-over-https,
  без второго PAT): создать ветку (refs API) → закоммитить файлы артефакта (Contents API
  `PUT /repos/.../contents/{path}`) → открыть PR (pulls API) → CI-гейт репо авто-сканирует →
  auto-merge при pass. cp-api ходит в api.github.com через OneCLI-прокси (PAT инжектится).
- **PR создаёт и мёржит сам агент** (не пользователь). Провенанс (автор-тенант, версия, наши
  scan-вердикты, commit-sha, PR#) → `artifact_versions.provenance` (jsonb) + `git_ref`.
- **Раскладка в репо:** артефакты в `skills/<name>/` (как сейчас) + новые `agents/<name>.md`,
  `commands/<name>.md`, `workflows/<name>/` (компилируется в commands+agents). НЕ трогаем sensitive
  paths (`.github/`, `users.yaml`, `deploy/`) → sensitive-paths-gate не срабатывает.
- **⚠️ Репо ПУБЛИЧНЫЙ** (`private:false`). Значит `visibility=private` артефакта enforced на уровне
  нашего реестра/Mini-App-UI (кого показываем в маркетплейсе), НЕ на уровне git (файлы физически
  публичны). Для MVP это ок (Vitaliy выбрал этот репо осознанно); если нужна git-приватность —
  отдельный приватный репо позже.

## 5. Модель данных — дельта
Таблицы есть. Нужна **миграция 0003** только для недостающих полей/связи:
- `artifacts`: + `description text`, + уникальный индекс `(owner_user_id, type, name)`.
- `artifact_versions`: + `status` (enum `draft|scanning|published|rejected`), + `scan_summary jsonb`.
- (опц.) `selective_grants` — отложено вместе с visibility=selective.
Никаких ломающих изменений; `installs.pinned_version` уже есть для пиннинга импорта.

## 6. API (роуты в cp-api — консистентно с sessions/mcp/approvals)
> doc 06 рисует отдельный `apps/registry`; для MVP на 2 тенанта это лишний процесс — делаю
> `registry-routes.ts` в cp-api, с прицелом на безболезненную экстракцию в свой сервис на M10.

```
GET  /registry/items?type=&visibility=          # каталог (мои + public)
GET  /registry/items/:id                         # карточка + версии + последние scan_results
POST /registry/publish    { artifactPath, type, name, version, visibility }
                                                 # → scanArtifact → judge → approval → agentPR → запись версии
POST /registry/import     { artifactVersionId }  # → ПОВТОРНЫЙ scanArtifact → approval → установка в .claude/
DELETE /registry/items/:id                        # снять с публикации (только owner; installs не трогаем)
```
Все мутации → audit (`registry.publish|import|reject`), вердикты → `scan_results`, через тот же
`tenantHome`-резолвинг, что и mcp-gate (мультибот-safe).

## 7. Поток ПУБЛИКАЦИИ (детерминированно, fail-closed)
1. Mini App: тенант выбирает артефакт (scoped FileTree, scope=project/artifacts) → «Опубликовать»
   → `POST /registry/publish`.
2. cp-api читает файлы артефакта из песочницы тенанта (`tenantHome/.claude/...` или `~/work`),
   нормализует в `ScanInput`, зовёт `scanArtifact` (deterministic → judge через Orchestrator,
   **worst-verdict-wins, fail/error → стоп + причина**). Вердикты → `scan_results`.
3. Pass → создаётся **approval-карточка** (M5.4b): «Опубликовать `<name>` v`<x>` как `<public|private>`?»
   — Telegram-нотификация токеном тенанта + ответ в Mini App. (Само-публикация своего же артефакта:
   approval опционален — открытый вопрос Q3.)
4. Allow → агент (cp-api под `cp-registry-bot` PAT) коммитит файлы в `fleet-artifacts`, открывает+мёржит
   PR, пишет `artifact_versions` (version, git_ref=sha, provenance, status=published). Audit.
5. Fail/Deny → `status=rejected` + причина в карточке; ничего не публикуется.

## 8. Поток ИМПОРТА (повторный скан — каждый импорт пересекает границу доверия)
1. Mini App «Маркетплейс» → выбор версии → `POST /registry/import`.
2. cp-api тянет **зафиксированную версию** из GitHub-org (по git_ref).
3. **ПОВТОРНЫЙ `scanArtifact`** под актуальные правила (ruleset мог измениться с публикации;
   judge-кэш по hash+ruleset+model сам решит hit/miss). Fail/error → отказ + причина, **ничего не ставим**.
4. Pass → approval-карточка «Импортировать в свою песочницу?» → allow → файлы пишутся в `.claude/`
   тенанта (skill→`skills/<name>/`, subagent→`agents/<name>.md`, command→`commands/<name>.md`,
   workflow→`commands/`+`agents/`), запись `installs` (pinned_version). Audit `registry.import`.
5. Конфликт имён — two-tap «перезаписать?» (как M5.5 overwrite). Платформенные скиллы/хуки —
   защищены, импорт их не перетирает (AgentShield-залок).

## 9. Безопасность / инварианты
- **LLM только событийно** (publish/import) — НЕ рекуррентно ([[feedback_no_recurring_llm_calls]]);
  judge — только через Orchestrator (кэш/single-pass/$-cap), без прямых вызовов сканеров.
- **Секреты только OneCLI**: org-PAT bound к `cp-registry-bot`, инжект на прокси; в БД/логах/аудите —
  никогда не значение.
- **Fail-closed везде**: judge-error == block (и публикации, и импорта).
- **Аудит append-only** на каждое решение (publish/import/reject/scan-verdict).
- **Граница доверия = импорт**: повторный скан обязателен, даже если на публикации был pass.
- **Пилот-only** ([[feedback_rearchitecture_pilot_vitaliy_only]]): фича за флагом тенанта; остальные боты не трогаем.

## 10. Mini App (UI)
- Экран **📦 Маркетплейс**: список (фильтр type/visibility), карточка (автор, версия, scan-бейдж,
  «Импортировать»).
- В FileTree/артефактах — кнопка **«Опубликовать»** на skill/agent/command/workflow.
- Карточки approval публикации/импорта — в существующей очереди approvals (web_app кнопка, как M5.4b).
- i18n `t()` (ru сейчас, en позже — как M5.10+).
- Деплой фронта = `pnpm build` + operator `cp -a dist → /var/www/miniapp/dist` ([[feedback_telegram_webview_index_cache]] — версионируем бандл).

## 11. Rollback-first ([[feedback_rollback_first_one_command_revive]])
- `install/m8.1-registry.sh apply|rollback` — миграция 0003, cp-api роуты, OneCLI-агент
  `cp-registry-bot` + bind org-PAT; rollback снимает роуты/агента (данные не трушим).
- GitHub-org изолирована: сбой пайплайна публикации НЕ влияет на работу бота (публикация —
  отдельный путь, не в hot-path сессии).
- Импорт пишет в `.claude/` через тот же git-HEAD-коммит механизм (ADR-003), что и mcp-gate →
  AgentShield-откат доступен.

## 12. Порядок сборки (после approve)
1. Миграция 0003 + расширение схемы.
2. GitHub-org `fleet-artifacts` + `cp-registry-bot` OneCLI-агент + org-PAT в vault (operator-step).
3. `registry-routes.ts` + publish-поток (scan→judge→approval→agentPR) + reuse approvals.
4. Import-поток (pull→re-scan→approval→install).
5. Mini App: Маркетплейс + «Опубликовать» + карточки.
6. `m8.1-accept.mjs` (e2e: publish skill → scan-pass → PR → import в «другого» → re-scan → install;
   + negative: артефакт с инъекцией → fail-closed, не публикуется/не ставится).

## 13. Критерии приёмки (M8.1)
- Публикация skill/subagent/command/workflow: scan-pass → агентский PR в org → версия+провенанс в реестре.
- Заведомо вредный артефакт (тест-инъекция) → **fail-closed**, не публикуется И не импортируется.
- Импорт делает повторный скан и ставит pinned-версию; конфликт → two-tap overwrite.
- Все вердикты/решения — в неизменяемом аудите; org-PAT нигде в файлах.
- Прогон на 2 тенантах: vitaliy публикует → dmrudenko импортирует (или наоборот).

## 14. Решения (все 5 закрыты Vitaliy 2026-06-16)
1. **Стор:** репо `joejoker77/claude-bot-skills` (не новая org). PAT — уже в OneCLI
   (`vitaliy-github-github_pat`, api.github.com, admin/push verified). Публикация — через REST API
   (commit+PR+merge), CI-гейт репо авто-сканирует. ⚠️ репо публичный → private = на уровне реестра. §4.
2. **MinIO зеркало:** DEFER. ✅
3. **Approval на публикацию:** НЕ требовать у админов; обязательно у остальных. Импорт — approval всегда. §3.
4. **Типы MVP:** skill/subagent/command/workflow; mcp/plugin — отдельным под-этапом. ✅
5. **visibility=selective:** DEFER; MVP = public/private. ✅

→ Все вопросы закрыты. Сборка начата 2026-06-16 (порядок — §12).

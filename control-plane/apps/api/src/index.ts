// @fleet/api — Mini App backend + public API (Fastify).
//
// M1 scope: initData-HMAC auth → POST /auth/session (JWT, Redis-tracked) and
// GET /me. All other endpoints from doc 07 (fs/build/registry/usage/sessions/
// approvals/live) are added in their own milestones (M5/M6/M8) and currently
// return 501 behind feature flags.
//
// Real implementation is filled in at M1.4. This placeholder keeps the
// workspace graph valid (dependencies declared) before that step.

export const PLACEHOLDER = "fleet-api M1.4 not yet implemented";
